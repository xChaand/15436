struct Point [
    integer x.
    integer y.
]
integer f[integer x, boolean b] [ ]
void g[] [
    integer a.
    boolean b.
    struct Point p.
    p:x = a.
    b = a == 3.
    f(a + p:y*2, b).
    g().
]

# my tests

struct Car [
    integer speed.
    boolean sold.
]

void func[integer x, boolean y] [
    struct Car c.
    if (x > 1) {
        struct Point p2.
        g().
    } else {
        struct Point p2.
        input -> p2:x.
        while (y) {
            disp <- x + 33.
            c:sold = TRUE.
            c:speed = 100.
        }
    }
]